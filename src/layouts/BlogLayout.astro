---
import MainLayout from "./MainLayout.astro";
const {frontmatter} = Astro.props
---

<MainLayout title={frontmatter.title} descryption="{frontmatter.tag}" >
    <h1>
        {frontmatter.title}
    </h1>
    <slot/>
</MainLayout>